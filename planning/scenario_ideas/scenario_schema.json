{
  "v": 2,
  "id": "string",
  "title": "string",
  "domain": "string",
  "desc": "string",

  "env": {
    "name": "string",
    "channel": "string",
    "init": {},
    "percepts": [
      { "name": "string", "args": ["string"], "scope": "agent|global" }
    ],
    "actions": [
      { "name": "string", "by": "AgentType", "pre": ["string"], "eff": ["string"] }
    ],
    "step_limit": 0
  },

  "agents": [
    {
      "type": "AgentClassName",
      "count": 1,
      "role": "string",
      "init": { "beliefs": ["string"], "goals": ["string"] },

      "plans": [
        { "on": "gain_belief|gain_goal|gain_percept", "key": "Kind:name(args)", "do": "behavior_name" }
      ],

      "behaviors": [
        { "name": "behavior_name", "steps": [ { "op": "string", "p": {} } ], "stop_after": false }
      ]
    }
  ],

  "term": {
    "mode": "event|steps",
    "step_limit": 0,
    "stop": ["string"]
  },

  "success": [
    { "name": "string", "def": "string" }
  ],

  "metrics": {
    "primary": ["string"],
    "secondary": ["string"]
  }
}